{
  "name": "playableos",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --filter @playableos/web dev",
    "dev:clean": "pnpm --filter @playableos/web dev:clean",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter @playableos/web... build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
